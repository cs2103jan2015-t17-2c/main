#include "TMExecutor.h"
int main () {
	/*while (true) {
	std::cout << "ENTER INPUT: " << std::endl;
	std::string input;
	std::getline(std::cin, input);
	TMExecutor* exe = TMExecutor::getInstance();
	std::vector<std::vector<std::string>> output;
	exe->executeMain(input);
	//std::vector<std::string> display = exe->getDisplayInfo();
	//std::cout << "SIZE OF DISPLAY " << display.size() << std::endl;
	std::vector<std::string>::iterator iter;
	for (iter = display.begin(); iter != display.end(); ++iter) {
		std::cout << *iter;
	}
	}
	system("pause");*/
	return 0;
	}